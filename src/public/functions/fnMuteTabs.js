async function muteTabs() {
  /* let audibleTabs = await getTabs({ audible: false }); // DESMUTAR TUDO
  FUNÇÃO TIPO CONTROLLER
  DESMUTAR TODO MUNDO TIRAR ACTIVE

    let audibleTabs = await getTabs({ active: false, audible: true }); // MUTAR TODAS 

  if(audibleTabs.length > 0) {
    audibleTabs.map(tabInfo => {
      let tabId = tabInfo.id;
      let isMuted = !tabInfo.mutedInfo.muted;
      
      chrome.tabs.update(tabId, { muted: isMuted });
      console.log((isMuted) ? 'Abas mutadas' : 'Abas desmutadas');
    });
  }*/
  console.log('FUNCIONA DEUS !!!');
}   

